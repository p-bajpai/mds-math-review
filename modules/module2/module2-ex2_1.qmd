---
format: html
---

<script src='../../src/quiz.js'></script>  

# 1.1. Exercises

## Understanding the Derivative

<div id='s1'></div> 
<script>
    generateQuiz(
        's1', 
        'For each of the plots below, select the function whose graph would match the plot.', 
        `
        <img src="../../static/module2/s1.png" width="500">
        <br><br>
        `, 
        { 
        'f(x,y) = x^2+y^2 - 4': 'Does the graph curve up or down if you fix x and look only in the y direction?',
        'f(x,y) = x -4y + 7': 'Try again!',
        'f(x,y) = x^2 - y^2 + 3': '',
        'f(x,y) = |x| + 3y': 'Not quite.',
        'f(x,y) = y^2 + \sin x': 'Try again!'
        },
        'f(x,y) = x^2 - y^2 + 3'
    );
</script>

<div id='s2'></div> 
<script>
    generateQuiz(
        's2', 
        '', 
        `
        <img src="../../static/module2/s2.png" width="500">
        <br><br>
        `, 
        { 
        'f(x,y) = x^2+y^2 - 4': 'Not quite.',
        'f(x,y) = x -4y + 7': 'Try again!',
        'f(x,y) = x^2 - y^2 + 3': 'Not quite',
        'f(x,y) = |x| + 3y': '',
        'f(x,y) = y^2 + \sin x': 'Try again!'
        },
        'f(x,y) = |x| + 3y'
    );
</script>

<div id='s3'></div> 
<script>
    generateQuiz(
        's3', 
        '', 
        `
        <img src="../../static/module2/s3.png" width="500">
        <br><br>
        `, 
        { 
        'f(x,y) = x^2+y^2 - 4': '',
        'f(x,y) = x -4y + 7': 'Try again!',
        'f(x,y) = x^2 - y^2 + 3': 'Does the graph curve up or down if you fix x and look only in the y direction?',
        'f(x,y) = |x| + 3y': 'Not quite.',
        'f(x,y) = y^2 + \sin x': 'Try again!'
        },
        'f(x,y) = x^2+y^2 - 4'
    );
</script>

<div id='s4'></div> 
<script>
    generateQuiz(
        's4', 
        '', 
        `
        <img src="../../static/module2/s4.png" width="500">
        <br><br>
        `, 
        { 
        'f(x,y) = x^2+y^2 - 4': 'Not quite.',
        'f(x,y) = x -4y + 7': 'Try again!',
        'f(x,y) = x^2 - y^2 + 3': 'Try again!',
        'f(x,y) = |x| + 3y': 'Not quite.',
        'f(x,y) = y^2 + \sin x': ''
        },
        'f(x,y) = y^2 + \sin x'
    );
</script>